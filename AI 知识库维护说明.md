# AI 知识库维护说明

> 返回 [[00. 知识库导航|知识库导航]]。本说明可以直接交给 Codex、Claude、ChatGPT 或其他具备文件访问能力的 AI，用于扩充和维护本 Obsidian 技术知识库。

## 目标

把技术资料整理为准确、结构清楚、能学习、能检索、能面试复习且可持续维护的 Obsidian 笔记。完成内容之外，还必须维护目录索引、语义关联和链接完整性。

## 可复用指令

下面内容可以作为新会话的系统说明或任务前置提示。使用时替换方括号变量。

```text
你是一个技术知识库维护助手，负责维护 Obsidian Vault。

本次主题：[主题或目录]
资料来源：[官方文档 URL、本地文件或“根据可靠公开资料”]
目标文件/目录：[Vault 内路径]
详细程度：[基础 / 完整 / 深入源码]

执行要求：
1. 先检查现有目录、同主题笔记、命名方式和 Obsidian 双链，不重复创建内容。
2. 当任务要求查阅资料时，优先官方文档、标准规范和项目仓库；区分稳定事实、版本相关行为与个人建议。
3. 内容至少说明：概念、解决的问题、核心机制、基本用法、示例、优势、限制、适用场景、注意事项和常见面试题。按主题需要增减，不能机械凑标题。
4. 示例必须自洽，包含必要 import、上下文和错误处理；不要留下 TODO、伪实现、省略号或不存在的 API。
5. 使用简体中文解释，API、命令、类型和文件名保留官方英文；Markdown 标题层级连续，代码块标注语言。
6. 使用 [[Vault/相对路径|显示名称]] 建立语义关联。链接到具体概念笔记，不只链接目录；同时更新所在目录索引。
7. 保留用户已有正文、附件、图片路径和手工双链。除非明确要求，不重命名、不移动、不删除文件。
8. 不手工修改 <!-- obsidian-nav:start --> 到 <!-- obsidian-nav:end --> 的自动导航区。
9. 新增或移动笔记后，在 Vault 根目录运行：node "工具/更新 Obsidian 关联.mjs"
10. 完成后检查：断开的双链、孤立笔记、未闭合代码块、重复标题、错误相对路径和未引用附件。
11. 最终只汇报实际修改、文件位置、验证结果和仍存在的限制，不声称执行过未执行的检查。
```

## 标准工作流

### 1. 发现上下文

- 列出目标目录和相邻目录；
- 搜索主题关键词、别名和已有双链；
- 阅读该目录中至少一篇代表性笔记，匹配现有结构和深度；
- 检查工作区是否有用户尚未提交的修改，不覆盖或回退无关内容；
- 确认目标是新增、扩充、导入、总结、审校还是仅回答问题。

### 2. 规划知识边界

先回答以下问题：

1. 这个主题解决什么问题，与相邻主题的边界是什么？
2. 哪些属于基础概念，哪些属于生产实践，哪些属于进阶原理？
3. 是否需要拆成多篇笔记？单篇过长且存在独立学习目标时再拆分；
4. 哪些内容会随版本变化，需要注明版本或查阅日期？
5. 读者应先掌握哪些内容，学完后应继续阅读什么？

### 3. 选择可靠来源

来源优先级：

1. 标准规范、语言/框架/工具官方文档；
2. 官方仓库、RFC、发布说明和维护团队文章；
3. MDN、web.dev 等高质量平台资料；
4. 有实证的工程文章和源码分析；
5. 搜索摘要、论坛回答只作为线索，不能直接作为事实依据。

涉及版本、兼容性、默认行为、安全策略和已废弃 API 时必须核对当前官方资料。不要把实验功能写成普遍可用，也不要把个人选型建议包装成官方结论。

### 4. 编写内容

常规技术笔记可从以下结构选择：

```markdown
# 主题

一句话定义，以及它解决的问题。

## 核心概念

## 工作原理

## 基础示例

## 核心 API/配置

## 生产实践

## 性能与安全

## 优势与限制

## 适用场景与选型

## 常见错误

## 面试题

## 关联阅读
```

这只是候选结构。基础概念、工具手册、源码文章和方案对比的组织方式不同，应以读者完成任务所需的信息结构为准。

### 5. 建立 Obsidian 关联

链接分为三层：

- 层级链接：总导航、领域知识地图和目录索引；
- 顺序链接：同一专题的上一篇/下一篇；
- 语义链接：正文中真正存在依赖、对比、实现或应用关系的概念。

链接格式统一使用 Vault 相对路径，避免同名文件歧义：

```markdown
[[前端/浏览器/3. JavaScript运行时与事件循环|事件循环]]
[[计算机基础/计算机网络|计算机网络]]
```

不要为了提高图谱连线数量，在每次出现关键词时重复添加双链。同一小节首次出现且确有阅读价值时链接一次即可。

新增笔记后执行：

```bash
node "工具/更新 Obsidian 关联.mjs"
```

脚本会：

- 为有内容的目录创建或更新 `00. 目录.md`；
- 更新总导航和领域知识地图中的自动目录区；
- 为内容笔记生成所属目录、领域入口、相邻笔记和总导航；
- 只替换 `obsidian-nav`/`obsidian-auto-index` 标记区，不改正文。
- 校验双链、孤立/不可达笔记、代码围栏、图片缺失和未引用附件；发现问题时输出明细并返回非零状态。

### 6. 验证

每次修改至少验证：

- 所有 `[[...]]` 目标真实存在，带 Alias 和 Heading 的链接正确解析；
- 每篇 Markdown 至少能通过一条路径到达总导航；
- 新笔记能从父目录索引进入，父目录能返回上级；
- Markdown 围栏代码块成对闭合；
- 示例没有缺少 import、未声明变量和明显错误 API；
- 本地图片、附件和嵌入路径存在；
- 没有 TODO、TBD、占位文本或无意义省略；
- 没有改动 `.obsidian` 工作区状态和无关文件。

## 写作规范

### 语言

- 使用简体中文，表达直接、可验证；
- 第一次出现缩写时给出英文全称或上下文；
- API 名、代码标识符和命令不翻译；
- 避免“显而易见”“非常简单”等没有信息量的表述；
- 比较方案时明确评价维度，不使用绝对的“最好”。

### 标题

- 每篇只有一个一级标题；
- 标题层级逐级下降，不从二级直接跳到四级；
- 标题表达知识问题，不使用“其他”“杂项”等不可检索名称；
- 面试题可以使用问题作为二级标题，答案紧随其后。

### 示例

- 示例尽量最小，但必须完整表达核心行为；
- 声明必要依赖、运行环境和关键版本差异；
- 涉及网络、文件、数据库、权限时包含失败和清理路径；
- 安全示例默认使用参数化、输出编码、最小权限和服务端校验；
- 不使用真实凭证、Token、私人域名和用户数据；
- 大段源码优先引用本地导入文章或拆分讲解，不复制整份官方文档。

### 对比

方案对比至少覆盖：定位、运行模型、生态、开发体验、性能、部署、限制、团队成本和适用场景。结论应从需求约束推导，而不是只看流行度。

## 文件与目录规范

- 同一系列使用 `1. 主题.md`、`2. 主题.md` 等编号；
- 库/框架专题可直接使用官方名称，例如 `Prisma.md`；
- 每个含笔记的目录由脚本维护 `00. 目录.md`；
- 领域根使用独立知识地图，例如 `前端/00. 前端知识地图.md`；
- 图片优先放在笔记同级的“图片和附件”目录，并使用相对嵌入；
- 移动或重命名文件前检查入链，并优先让 Obsidian 自动更新链接；
- 不创建内容为空的占位笔记。

## 自动导航标记

以下区域由脚本维护：

```html
<!-- obsidian-nav:start -->
...
<!-- obsidian-nav:end -->

<!-- obsidian-auto-index:start -->
...
<!-- obsidian-auto-index:end -->
```

AI 可以修改标记区外的正文和人工语义链接。若导航结构需要变化，应修改 `工具/更新 Obsidian 关联.mjs` 后重新生成，而不是逐篇手改自动内容。

## 导入本地文档

导入 Markdown 或文件夹文章时：

1. 先检查原文编码、图片引用、同名文件和目录结构；
2. 保留正文信息，不把导入变成过度概括；
3. 按用户要求移除来源头尾、广告、重复目录或无关结语；
4. 修复图片为 Vault 内相对路径，复制附件前确认不会覆盖同名资源；
5. 统一标题层级、代码围栏和表格，但不改变技术含义；
6. 在适合的位置补充背景、风险、示例和语义双链；
7. 更新目录索引并运行完整链接检查。

## 维护边界

- 未经要求不删除、重命名或移动用户文件；
- 未经要求不修改 `.obsidian` 配置、主题或插件；
- 不把网络搜索结果、生成内容或命令输出伪装成官方原文；
- 无法访问官方资料时明确说明，并只写稳定知识；
- 遇到内容冲突时保留用户原文，标注问题或请求确认；
- 批量修改前先统计范围，批量修改后报告实际数量。

## 完成报告模板

```text
已完成 [主题/目录]：

- 新增：X 篇；更新：Y 篇；建立双链：Z 条；
- 覆盖：核心概念、示例、生产注意事项、方案对比和面试题；
- 导航：目录索引、上一篇/下一篇、领域入口已更新；
- 验证：断链 0、孤立笔记 0、未闭合代码块 0；
- 限制：未能验证或仍需人工确认的内容。
```

报告中的数量必须来自实际检查，不能估算。
